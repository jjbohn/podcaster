defmodule Podcaster.GuardianSerializerTest do
  use ExUnit.Case, async: true

  alias Podcaster.GuardianSerializer
  alias Podcaster.User

  # test "for_token :ok" do
  #   user = %User{id: "10"}
  #   assert GuardianSerializer.for_user(user) == { :ok, "User:10" }
  # end

  # test "for_token :error" do
  #   assert GuardianSerializer.for_user("foo") == { :error, "Unknown resource type" }
  # end

  # test "from_token :ok" do
  # end

  # test "from_token :error" do
  #   assert GuardianSerializer.from_token("foo") == { :error, "Unknown resource type" }
  # end
end
